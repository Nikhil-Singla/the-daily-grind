class Solution {
public:
    int romanToInt(string s) 
    {
        unordered_map<char, int> conversion;
        conversion['I'] = 1;     
        conversion['V'] = 5;
        conversion['X'] = 10;
        conversion['L'] = 50;
        conversion['C'] = 100;
        conversion['D'] = 500;
        conversion['M'] = 1000;
        for(int i = 0; i < s.length() - 1; i++)
        {
            
        }
    }
};