class Solution {
public:

    void init

    string convert(string s, int numRows) 
    {
        if(numRows == 1)
        {
            return s;
        }
        int len = s.size();
        char ans[numRows][len/2];
        
        init(char);
        
        for(int counter = 0; counter<len; counter++)
        {
            
        }
    }
};