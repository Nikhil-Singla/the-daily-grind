class Solution {
public:
    string longestPalindrome(string s) {
        if(s.length()==1)
        {
            return s;
        }
        auto first = s.begin(), last=s.end();
        for(auto mid=s.begin();mid<last;mid++)
        {
            
        }
        return "a";
    }
};
